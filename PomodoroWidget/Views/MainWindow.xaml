<Window x:Class="PomodoroWidget.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PomodoroWidget.ViewModels"
        xmlns:views="clr-namespace:PomodoroWidget.Views"
        Title="Pomodoro Widget"
        Width="360" Height="580"
        MinWidth="300" MinHeight="200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Border Background="{StaticResource BgPrimaryBrush}"
            CornerRadius="14"
            BorderBrush="#10FFFFFF" BorderThickness="1"
            Margin="8">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="12" Opacity="0.4" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="Transparent"
                    MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="🍅 Pomodoro Widget"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="13" FontWeight="SemiBold" Opacity="0.85"
                               VerticalAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="{Binding IsExpanded, Converter={StaticResource BoolToVis}, ConverterParameter=toggle}"
                            Command="{Binding ToggleExpandCommand}"
                            Style="{StaticResource WidgetButton}"
                            Width="28" Height="28" Padding="0"
                            FontSize="14" ToolTip="Toggle todo list">
                        <Button.ContentTemplate>
                            <DataTemplate>
                                <TextBlock Text="☰" FontSize="14"/>
                            </DataTemplate>
                        </Button.ContentTemplate>
                    </Button>

                    <Button Grid.Column="2"
                            Style="{StaticResource WidgetButton}"
                            Width="28" Height="28" Padding="0"
                            Click="CloseButton_Click" ToolTip="Close">
                        <TextBlock Text="✕" FontSize="12" Opacity="0.6"/>
                    </Button>
                </Grid>
            </Border>

            <Border Grid.Row="0" BorderBrush="#10FFFFFF" BorderThickness="0,0,0,1"
                    VerticalAlignment="Bottom"/>

            <!-- Timer -->
            <views:TimerView Grid.Row="1" DataContext="{Binding Timer}"/>

            <!-- Todo List -->
            <views:TodoListView Grid.Row="2" DataContext="{Binding Todo}"
                                Visibility="{Binding DataContext.IsExpanded, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BoolToVis}}"/>
        </Grid>
    </Border>
</Window>
